services:
  backend:
    build:
      context: ./backend
    ports:
      - "6688:6688"

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE_URL=http://localhost:6688
    depends_on:
      - backend